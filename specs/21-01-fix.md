<!-- SPDX-License-Identifier: MPL-2.0 -->

# Spec 21-01: Skewed `andCardinality` fix (gated on `21-00`)

Second chunk of [skewed `andCardinality`](21-skewed-andcardinality.md). **Authored around
`21-00`'s attribution** — no lever preselected here.

## Gate — do not start until

- `21-00` attributes the ~1.46x and names a **reachable, generalizing improvement**. If it is
  specific to 32 × 4096 all-hit, or intrinsic to a count-only galloping kernel at ~6 µs with
  no clean improvement, this chunk is **not written** — parity is effectively complete and the
  residual is documented.

## Lever — follows the attribution

- **Kernel dominates** → a tighter galloping-count kernel (`intersectCardGallop`).
- **Traversal/dispatch dominates** → target the per-container dispatch / key-traversal path
  instead; the kernel is fine.
- **Threshold (boundary cases only)** → a count-only crossover re-tune; it cannot move the
  32 × 4096 point, so only worthwhile if `21-00` shows boundary-case losses. Keep it separate
  from the write-kernel threshold if they differ.

## Constraints

- **Correctness:** `andCardinality` result unchanged (`== |a ∩ b|`); the array-kernel
  differential coverage (`bench_aa` cross-checks gallop / merge / SIMD for identical results)
  stays green; the write-kernel dispatch is untouched unless the diagnosis justifies it.
- **Architecture test scope:** a change to a **shared kernel or the x86/scalar threshold**
  **requires x86 testing**; a change to **only the measured NEON threshold** is explicitly
  **scoped to NEON**.
- **No regression** at other skew ratios or on the balanced-array kernels (spec 11).
  Allocator-independent (count-only path) — pure kernel/threshold/dispatch work.

## Acceptance

- The isolated skewed `andCardinality` ratio moves toward parity **at the original
  32 × 4096 case** (and across the sweep points the attribution implicated), no regression
  elsewhere, differential green.
- Validation: `zig build test`; `zig build bench-aa -Dcpu=native && ./zig-out/bin/bench_aa`;
  `zig build difftest`; the focused five-process runner; env header recorded on
  `ReleaseFast`/native/M4.
- Given the ~6 µs absolute, a **partial improvement or a documented "intrinsic at this size"
  is an acceptable terminal outcome** — do not over-invest.

## Note

Fill in the concrete lever once `21-00` reports, or delete this chunk if `21-00` records a
non-generalizing / intrinsic result.
